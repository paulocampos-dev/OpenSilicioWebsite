FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better caching)
COPY package*.json ./
RUN npm install

# Source code will be mounted as volume
# Don't copy source code here for better hot reloading

EXPOSE 5173

# Vite dev server with host exposed
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

